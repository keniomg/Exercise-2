using System.Collections;
using System;
using System.Text;
namespace lessons
{
    class program
    {
        static void Main()
        {
            int firstNumber = 5;
            int secondNumber = 12;
            int deltaNumber = secondNumber - firstNumber;
            int lastNumber = 96;

            for (int i = firstNumber; i <= lastNumber; i += deltaNumber)
            {
                Console.WriteLine(i);
            }
        }
    }
}
